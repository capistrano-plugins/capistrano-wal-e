require 'spec_helper'

RSpec.describe Capistrano::Wal::E do
  it 'has a version number' do
    expect(Capistrano::Wal::E::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
